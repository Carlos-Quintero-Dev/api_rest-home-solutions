/* This TypeScript code snippet is defining an enum named `ROLES` with three members: `COMPRADOR`,
`VENDEDOR`, and `ADMIN`. Each member is assigned a string value that is the same as the member name
itself. */
export enum ROLES{
    COMPRADOR="COMPRADOR",
    VENDEDOR = "VENDEDOR",
    ADMIN="ADMIN"
}